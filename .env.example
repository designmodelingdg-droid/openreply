# App
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace-with-a-strong-random-secret
CRON_SECRET=replace-with-a-strong-random-secret
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Database / queue
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/openreply
REDIS_URL=redis://localhost:6379

# Email magic links (Resend)
RESEND_API_KEY=re_...
EMAIL_FROM=OpenReply <login@example.com>

# Optional: restrict who can sign in. Without it, anyone who reaches your
# public URL can request a magic link and gets their own workspace. Comma
# separated, case insensitive.
# ALLOWED_EMAILS=you@example.com,teammate@example.com

# Optional: use your own SMTP server instead of Resend. When EMAIL_SERVER is
# set it takes over and RESEND_API_KEY is not needed. URL-encode special
# characters in user and password (@ becomes %40).
# EMAIL_SERVER=smtps://login%40example.com:password@mail.example.com:465

# Meta / Instagram
META_GRAPH_API_VERSION=v25.0
INSTAGRAM_APP_ID=your-instagram-app-id
INSTAGRAM_APP_SECRET=your-instagram-app-secret
FACEBOOK_APP_SECRET=your-facebook-app-secret
WEBHOOK_VERIFY_TOKEN=replace-with-a-webhook-verify-token

# Optional: follow-gate handoff. When another app owns the Instagram DM thread
# (e.g. GoHighLevel driving a sales bot), Meta rejects every message OpenReply
# sends after the first one. With these set, a follow-button tap is verified
# with Meta and the outcome is POSTed here instead, so the thread owner sends
# the reply. Leave unset to keep sending through Meta as before.
# FOLLOW_HANDOFF_URL=https://your-bot.example.com/webhook/openreply
# FOLLOW_HANDOFF_SECRET=replace-with-a-strong-random-secret

# Optional: branding for the web follow-gate page (per campaign, "verify on a
# web page"). That page is the only OpenReply screen a commenter ever sees, and
# it opens right after one of their DMs, so it should look like the account
# that sent it. Defaults ship with the Design Modeling Academy brandkit.
# FOLLOW_GATE_LOGO_URL=https://example.com/your-logo.png
# FOLLOW_GATE_COLOR_PRIMARY=#003e5c
# FOLLOW_GATE_COLOR_ACCENT=#ca7520
